# pending.md — facts waiting for tier decision

# When MEMORY.md hits 10,000 chars, new facts are buffered here instead of being added to hot.
# On review, decide for each fact:
#   - promote to hot MEMORY.md (universal rule, applies every turn)
#   - add to an existing warm/<topic>.md file (project-specific context)
#   - create a new warm/<topic>.md file
#   - archive to cold.md (one-off, low priority)
#   - drop (turns out not important)

# Format: append each fact with date + source for context.

# ---
